<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/trackContainer"
    android:layout_width="match_parent"
    android:layout_height="@dimen/Track.Height"
    android:gravity="center_vertical"
    android:orientation="horizontal">

    <ImageView
        android:id="@+id/albumCover"
        android:layout_width="@dimen/big_150"
        android:layout_height="@dimen/big_150"
        android:importantForAccessibility="no" />

    <LinearLayout
        style="@style/Weight.Horizontal.1"
        android:gravity="center_vertical"
        android:orientation="vertical"
        android:paddingHorizontal="@dimen/small_100">

        <TextView
            android:id="@+id/titleTextView"
            style="@style/Text.Regular.16"
            android:layout_width="match_parent"
            android:maxLines="1"
            android:textColor="?attr/colorOnSurface" />

        <TextView
            android:id="@+id/artistAndTimeTextView"
            style="@style/Text.Regular.11"
            android:layout_width="match_parent"
            android:ellipsize="middle"
            android:maxLines="1"
            android:textColor="?attr/colorOnSurfaceVariant" />
    </LinearLayout>

    <ImageView
        style="@style/Icon"
        android:contentDescription="@string/open_track"
        android:src="@drawable/ic_arrow_forward_24"
        app:tint="?attr/colorOnSurfaceVariant" />
</LinearLayout>